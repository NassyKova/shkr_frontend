import styled from "styled-components";

// Define a new styled component called Title, which will render an <h1>
const Title = styled.h1`
    color: var(--white);
    text-align: center;
    text-shadow: 2px 2px black;
`;

export default Title;
